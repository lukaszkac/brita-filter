reset_filter:
  name: Reset filter
  description: Mark the filter as replaced today and reset the counter.
  fields:
    entry_id:
      name: Filter
      description: Which filter to reset. Leave empty to reset all configured filters.
      required: false
      selector:
        config_entry:
          integration: brita_filter
